<div class="flex items-center">
    <input type="radio" wire:model.live="{{ $model }}" id="{{ $id }}" value="{{ $value }}" class="form-check-input">
    <label for="{{ $id }}" class="ml-2">{{ $label }}</label>
</div>
